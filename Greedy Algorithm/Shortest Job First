class Solution {
  public:
    long long solve(vector<int>& bt) {
        sort(bt.begin(),bt.end());
       int n = bt.size();
       int primary=0,wt=0;
       for(int i=0;i<n;i++){
          wt += primary;
          primary += bt[i];
                 }
                 return wt/n;
    }
};
