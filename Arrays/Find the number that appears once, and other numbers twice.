class Solution {
public:
    int singleNumber(vector<int>& nums) {
        
        int n=nums.size();
        int theOne=0;
        for(int i=0;i<n;i++){
           theOne = theOne ^ nums[i];
        }
        return theOne;
    }
};
