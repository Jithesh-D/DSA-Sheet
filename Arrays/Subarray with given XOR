int Solution::solve(vector<int> &A, int B) {
    int n = A.size();
   
    int count = 0;
    for(int i=0;i<n;i++){
         int XOR=0;
        for(int j=i;j<n;j++){
          XOR = XOR ^ A[j];  if(XOR == B){
            count++;
        }
        }
        
    }
    return count;
}
